---
title: "Collections"
---

extends /layouts/default.pug

block content

  p Articles with friends.

  .cards
    each collection in collections
      section.card
        h2 #{collection.title}

        p !{collection.description}

        ul
          //- TODO replace with relational data
          - const name = collection.file.segments[1];
          each post in posts.filter(p => p.collection === name)
            li: p #[a(href=urlFor(post)) #{post.title}]
