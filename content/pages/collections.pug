---
title: "Collections"
---

extends /layouts/default.pug

block content

  p Articles with friends.

  .cards
    each collection in store.allBy("collection")
      section.card
        h2 #{collection.title}

        p !{collection.description}

        ul
          //- TODO replace with relational data
          - const name = collection.file.segments[1];
          each post in store.allBy("post").filter(p => p.collection === name).sort()
            li: p #[a(href=urlFor(post)) #{post.title}]
